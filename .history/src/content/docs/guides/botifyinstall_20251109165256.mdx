---
title: Botify installation with Docker
description: A guide in my new Starlight docs site.
---

import { Steps } from "@astrojs/starlight/components";
import { Badge } from "@astrojs/starlight/components";

### Upload the Project Files

<Steps>

1. Upload the Project Files
   First, upload the botify.zip file to your VPS using FTP, SCP, or your preferred file transfer method

   ```js
   scp botify.zip root@your-server-ip:/var/www/

   ```

2. Unzip the Project
   Once uploaded

   ```
   cd /var/www/
   sudo unzip botify.zip -d botify

   ```

3. Navigate to the Project Directory

   ```
   cd /var/www/botify
   ```

4. Start the Application Using Docker

   ```
   sudo docker compose build up -d

   ```

5. Verify Containers are Running

   ```
   sudo docker ps

   ```

   You should see containers for:

   - botify-web (Next.js app)

   - redis

   - postgres

6. Access the Application

   ```
   http://yourdomain.com
   You will be automatically redirected to the Onboarding Page.

   ```

   <Badge text="Success" variant="success" />

   - Your Botify Bulk WhatsApp Sender is now successfully installed and running in a secure, Dockerized environment.

7. Create Admin Acoount

   ![Onboarding](/onboard1.png)

   From Here, you can create your first user account, which will be assigned administrator privileges. This admin account will allow you to manage the system, add Plans, and configure your WhatsApp sending settings.

</Steps>
### Botify Installation without Docker
<Steps>
1. Upload the project  files
   Upload the Project Files First, upload the botify.zip file to your VPS using FTP, SCP, or your preferred file transfer method
   
   ```js
   scp main.zip root@your-server-ip:/var/www/
   ```
2. Unzip the Project Once uploaded

    ```
    cd /var/www/
    sudo unzip botify.zip -d botify

    ```

3.  Navigate to the Project Directory

    ```
    cd /var/www/botify
    ```

4.  Update the .env File
    Open the `.env` file located in the root directory and update it with the correct credentials and configuration values

    ```
    BETTER_AUTH_SECRET=secret # Replace with a strong secret in production
    BETTER_AUTH_URL=http:/localhost:3000
    DATABASE_URL="postgresql://myuser:mypassword@localhost:5432/my_database"
    GOOGLE_CLIENT_ID=
    GOOGLE_CLIENT_SECRET=
    NEXT_PUBLIC_SOCKET_URL=http://localhost:3001 # Update to your socket server URL default: http://localhost:3001
    NEXTJS_URL=http://localhost:3000 # Update to your Next.js app URL
    NEXT_PUBLIC_APP_URL=http://localhost:3000 # Update to your Next.js app URL
    REDIS_HOST=localhost
    REDIS_PORT=6379
    SMTP_HOST=smtp.example.com
    SMTP_PORT=465
    SMTP_SECURE=true
    SMTP_USER=
    SMTP_PASS=
    SMTP_FROM=

    ```

5.  Copy the updated `.env` file to the following locations

         ```
         - apps/socket
         - apps/webs
         - apps/wserver
         ```

6.  Run the following command to install all project dependencies

              ```
              pnpm i
              ```

7.  Execute the initial database migrations using the command

                ```
                pnpm migrate
                ```

8.  Change directory to the database package:

                    ```
                    cd packages/db
                    ```

    </Steps>
