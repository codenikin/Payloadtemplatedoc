---
import { StarlightContent } from "@astrojs/starlight/components";

export async function getStaticPaths() {
  return [
    { params: { slug: ["getting-started"] } },
    { params: { slug: ["installation"] } },
    { params: { slug: ["usage"] } },
  ];
}
---

<StarlightContent />
